#include <stdio.h>

int main(){
	
	int number;
	int max = 0;
	int max_index;
	
	scanf("%d", &number);
	if(number > max){
		max = number;
		max_index = i
	}
	
	printf("%d", max);
	printf("%d", max_index);
	
	return 0;
}
